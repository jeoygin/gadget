cmake_minimum_required(VERSION 2.8)
project( equalize-image )
find_package( OpenCV REQUIRED )
add_executable( equalize-image equalize-image.cpp )
target_link_libraries( equalize-image ${OpenCV_LIBS} )
